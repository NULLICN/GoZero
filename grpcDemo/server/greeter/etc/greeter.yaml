# === Bootstrap config (local) ===
# Only contains connection info for Nacos config center.
# The main business config is stored in Nacos config center.

NacosIp: 8.137.180.112
NacosPort: 8848
NacosNamespaceId: public
NacosUsername:
NacosPassword:
ConfigDataId: greeter.rpc.yaml
ConfigGroup: NULLICN
