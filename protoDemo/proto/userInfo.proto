syntax = "proto3";
package userinfo;
option go_package = "./userService"; // ./生成路径;包名 eg: "./proto;userinfo"

message userinfo  { 
    string username = 1;
    int32 age = 2; 
    repeated string hobby = 3;
}
/* message SayHelloResp { 
    string message = 1; 
}

service Greeter {
    rpc SayHello(userinfo) returns(SayHelloResp);
} */